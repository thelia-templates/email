{% set create_url = thelia_url('/admin/password-create/' ~ (token is defined ? token : '')) %}
{{ 'Hello,'|trans({}, 'email', locale) }}

{{ 'You have requested a new password for your administrator account at %store_name%'|trans({'%store_name%': config('store_name')}, 'email', locale) }}.

{{ 'Please click here to define a new password: %url% . You will be prompted to enter a new password.'|trans({'%url%': create_url}, 'email', locale) }}

{{ 'If you don\'t requested a new password, please ignore this message.'|trans({}, 'email', locale) }}.

{{ 'Have a nice day'|trans({}, 'email', locale) }}
