{% set reset_url = thelia_url('/password/reset/%token', {token: token|default('')}) %}
{{ 'Hello,'|trans({}, 'email', locale) }}

{{ 'You asked to choose a new password for your account at %store_name%'|trans({'%store_name%': config('store_name')}, 'email', locale) }}.

{{ 'Click the link below, or copy it in your browser address bar, to choose it.'|trans({}, 'email', locale) }}

{{ reset_url }}

{{ 'This link can only be used once, and stops working %minutes% minutes after this email was sent.'|trans({'%minutes%': tokenLifetimeInMinutes|default(60)}, 'email', locale) }}

{{ 'If you did not ask for this, you can ignore this email: your password has not been changed.'|trans({}, 'email', locale) }}

{{ 'Kind regards'|trans({}, 'email', locale) }},
{{ 'The %store% team.'|trans({'%store%': config('store_name')}, 'email', locale) }}
